<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>Information</h1>
    <h2><%=user.firstName%></h2>
    <h2><%=user.lastName%></h2>
    <h2><%=user.email%></h2>
    <% if(user.mobileNo.length === 10) { %>
        <p><%=user.mobileNo%></p>
    <%} else{ %>
        <p>Sorry Your MobileNo Is Not Currect</p>
        <% } %>    
    <h2><%=user.address.line1%></h2>
    <h2><%=user.address.line2%></h2>
    <h2><%=user.address.pincode%></h2>
</body>
</html>